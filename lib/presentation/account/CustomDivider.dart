import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class myDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      thickness: 5,
      color: Color.fromARGB(60, 90, 88, 88),
    );
  }
}
